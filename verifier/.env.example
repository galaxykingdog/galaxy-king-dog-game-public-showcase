PORT=8787
CLUSTER=devnet
RPC_URL=https://api.devnet.solana.com

# Must match src/chain/chain_config.js poolRecipient
POOL_RECIPIENT=REPLACE_WITH_YOUR_POOL_WALLET

ASSETS_DIR=../assets
GATE_D_MODE=strict
ALLOW_LEGACY_NO_TICKET=false
RUN_TICKET_TTL_MS=1200000

# Optional: base64-encoded 64-byte ed25519 secret keys
VERIFIER_SECRET_KEY_B64=
SUBMITTER_SECRET_KEY_B64=

# Optional: lock accepted game id
EXPECTED_GAME_ID=
